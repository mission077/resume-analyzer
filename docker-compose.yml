version: "3.8"

services:
  # PostgreSQL Database
  postgres:
    image: postgres:13-alpine
    container_name: resume_analyzer_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-resume_analyzer}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - resume-analyzer-network

  # Next.js Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: resume_analyzer_app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-password}@postgres:5432/${POSTGRES_DB:-resume_analyzer}
      JWT_SECRET: ${JWT_SECRET}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      GOOGLE_GENERATIVE_AI_API_KEY: ${GOOGLE_GENERATIVE_AI_API_KEY}
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:3000}
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET}
      NODE_ENV: production
      NEXT_PUBLIC_LATEX_API_URL: http://latex-compiler:8001
      MARKITDOWN_API_URL: http://markitdown-api:8000
    depends_on:
      postgres:
        condition: service_healthy
      latex-compiler:
        condition: service_started
      markitdown-api:
        condition: service_started
    volumes:
      - ./public/uploads:/app/public/uploads
    networks:
      - resume-analyzer-network

  # LaTeX Compiler Service
  latex-compiler:
    build:
      context: ./services/latex-compiler
      dockerfile: Dockerfile
    container_name: resume_analyzer_latex
    restart: unless-stopped
    ports:
      - "8001:8001"
    networks:
      - resume-analyzer-network

  # MarkItDown API Service (PDF Parser)
  markitdown-api:
    build:
      context: ./services/markitdown-api
      dockerfile: Dockerfile
    container_name: resume_analyzer_markitdown
    restart: unless-stopped
    ports:
      - "8000:8000"
    networks:
      - resume-analyzer-network

volumes:
  pgdata:
    driver: local

networks:
  resume-analyzer-network:
    driver: bridge
